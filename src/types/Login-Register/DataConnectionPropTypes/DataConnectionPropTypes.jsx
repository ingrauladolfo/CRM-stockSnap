// DataConnectionPropTypes.js
import PropTypes from "prop-types";

const DataConnectionPropTypes = {
    pathTitle: PropTypes.string,
    title:PropTypes.string,
    text: PropTypes.string,
};

export default DataConnectionPropTypes;
